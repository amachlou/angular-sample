import { CurrencySigneDirective } from './currency-signe.directive';

describe('CurrencySigneDirective', () => {
  it('should create an instance', () => {
    const directive = new CurrencySigneDirective();
    expect(directive).toBeTruthy();
  });
});
